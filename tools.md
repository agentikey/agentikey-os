# Tools — Integrations Registry

One row per external tool the OS touches. Updated whenever a branch starts using a new tool.

**Mechanism** is one of: `mcp` (Claude MCP server), `cli` (command-line), `script` (local script in `automation/` or a branch), `api-key` (env var + reference doc), `manual` (human action), `not-yet-connected`.

| Domain | Tool | Mechanism | Auth location | Reference doc | Last checked |
|---|---|---|---|---|---|
| _example_ | _Obsidian_ | _filesystem_ | _local vault path_ | _references/obsidian.md_ | _2026-05-12_ |

(Fill in real rows during `/launch` and as branches wire new tools.)
