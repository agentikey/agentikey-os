# Cadence — `<branch-name>`

List every recurring trigger this branch owns. If a system here runs on a schedule, it should also appear in `automation/schedules.md`.

| System | Cadence | Trigger | Notes |
|---|---|---|---|
| _example_ | _scheduled_ | _cron:0 7 * * 1_ | _Monday 7am — feeds the weekly review_ |

If this branch has no recurring triggers, write: `none yet`.
